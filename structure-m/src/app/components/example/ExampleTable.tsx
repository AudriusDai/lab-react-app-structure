import React, { FC } from "react";

interface IProps {}

const ExampleTable: FC<IProps> = (props) => {
  return <div>Example table.</div>;
};

ExampleTable.defaultProps = {};

export default ExampleTable;
